---
import CardExperience from "../../components/CardExperience.astro";
import CardExperienceComponnent from "../../components/react/CardExperience";
---

<section class="flex items-center flex-col justify-center w-full gap-5 pt-14">
  <!-- <CardExperience /> -->
  <div class="w-[850px] flex justify-start items-center p-1">
    <label for="" class="text-white font-semibold text-2xl"
      >Experiencia Laboral</label
    >
  </div>
  <CardExperienceComponnent client:load />
  <CardExperienceComponnent client:load />
  <CardExperienceComponnent client:load />
  <!-- <div class="flex px-96 mx-20 py-12 lg:px-52">
    <div class="flex flex-col gap-5 items-start w-full">
      <span class="text-white font-bold text-3xl">Experiencia</span>
      <div class="flex flex-col gap-8">
        <CardExperience />
      </div>
    </div>
  </div> -->
</section>
